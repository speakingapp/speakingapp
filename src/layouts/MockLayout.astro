---
import { SEO } from "astro-seo";
import "@fontsource-variable/inter/index.css";

export interface Props {
  title: string;
}

const canonicalURL = new URL(Astro.url.pathname, Astro.site).toString();

const resolvedImageWithDomain = new URL(
  "/favicon.png",
  Astro.site
).toString();

const { title } = Astro.props;

const makeTitle = title
  ? title + " | " + "Edumo"
  : "SpeakingApp | Edumo Academy";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="google-site-verification" content="oG1MHMRxcH8M8WjcfaAYEqq-6Fv06LDOWF6NwaVg4rA" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.png" />
    <link rel="stylesheet" href="https://unpkg.com/onsenui/css/onsenui.css">
    <link rel="stylesheet" href="https://unpkg.com/onsenui/css/onsen-css-components.min.css">
    <script src="https://unpkg.com/onsenui/js/onsenui.min.js" is:inline></script>
    <script src="https://unpkg.com/jquery/dist/jquery.min.js" is:inline></script>
   <!-- Yandex.RTB -->
<script is:inline>window.yaContextCb=window.yaContextCb||[]</script>
<script src="https://yandex.ru/ads/system/context.js" async is:inline></script>
    <link rel="stylesheet" href="/static/css/main.css">
    <meta name="generator" content={Astro.generator} />

    <SEO
      title={makeTitle}
      description="SpeakingApp - Multilevelga tayyorgarlik ko'rish uchun onlayn platforma"
      canonical={canonicalURL}
      twitter={{
        creator: "@javlon_developer",
        site: "@speakingapp",
        card: "summary_large_image",
      }}
      openGraph={{
        basic: {
          url: canonicalURL,
          type: "website",
          title: `SpeakingApp - Multilevelga tayyorgarlik ko'rish uchun onlayn platforma`,
          image: resolvedImageWithDomain,
        },
        image: {
          alt: "SpeakingApp",
        },
      }}
    />
  </head>
  <body>
    <slot />
    <style is:global>
      /* Improve Page speed */
      /* https://css-tricks.com/almanac/properties/c/content-visibility/ */
      img {
        content-visibility: auto;
      }
       
body {
  --sb-track-color: #232E33;
  --sb-thumb-color: #888888;
  --sb-size: 10px;

  scrollbar-color: var(--sb-thumb-color) 
                   var(--sb-track-color);
}

body::-webkit-scrollbar {
  width: var(--sb-size) 
}

body::-webkit-scrollbar-track {
  background: var(--sb-track-color);
  border-radius: 10px;
}

body::-webkit-scrollbar-thumb {
  background: var(--sb-thumb-color);
  border-radius: 10px;
  
  }
    </style>

<!-- Yandex.RTB R-A-2860373-1 -->
<div id="yandex_rtb_R-A-2860373-1"></div>
<script is:inline>window.yaContextCb.push(()=>{
	Ya.Context.AdvManager.render({
		"blockId": "R-A-2860373-1",
		"renderTo": "yandex_rtb_R-A-2860373-1",
		"type": "feed"
	})
})
</script>
  </body>
</html>
